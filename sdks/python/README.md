# valet-agent

Python SDK for [Valet](https://github.com/joalavedra/valet) — agents hold
handles, never secrets.

```bash
pip install 'valet-agent[browser-use]'   # adds the browser-use adapter
```

See the repo README for usage.
